# import model
import pygame
import os

# define const
game_base_setup = {
    "FPS" : 60, 
    "WIDTH" : 1024,
    "HEIGHT" : 512,
    "GAMENAME" : 'UNTITLED', 
    "MAP_SIZE" : (2, 2)
}

# define color area
COLOR = {
    "BLACK" : (0, 0, 0),
    "AZURE" : (240,255,255)
}
